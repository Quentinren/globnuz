import React, { useState, useEffect, useRef } from 'react';


const NuzDynamics = ({ newsEvents }) => {

  return(
    <Grid row={5}>
      <Card></Card>
    </Grid>
  )
 
  
};

export default NuzDynamics;